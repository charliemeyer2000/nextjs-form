{
  "compilerOptions": {
    "baseUrl": "./", // Specify the correct base URL
    "paths": {
      "@/*": ["*"] // Map '@/*' to all files and folders under the base URL
    }
  },
  "include": ["./**/*"] // Make sure to include all files under the base URL
}
